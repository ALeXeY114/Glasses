/*$(function () {

	var nav__product = $("#navproduct"),
		asideH = $("#aside").innerHeight(),
	scrollOffset = 0;

	$(window).on("scroll", function(){

	
		scrollOffset = $(this).scrollTop();

		if(scrollOffset >= asideH) {
			navproduct.addClass("fixed");
		} else {
			navproduct.removeClass("fixed");
		}

		});
});*/


